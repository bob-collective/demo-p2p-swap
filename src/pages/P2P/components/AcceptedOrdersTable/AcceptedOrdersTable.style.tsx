import { CTA, Card, Span } from '@interlay/ui';
import styled from 'styled-components';

const StyledCTA = styled(CTA)`
  white-space: nowrap;
`;

const StyledCard = styled(Card)`
  overflow: auto;
`;

const StyledSpan = styled(Span)`
  white-space: nowrap;
`;

export { StyledCTA, StyledSpan, StyledCard };
